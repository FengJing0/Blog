import styled from "styled-components"

export const Time = styled('p')`
  margin:0;
  font-size:14px;
`

export const TimeItem = styled('p')`
  margin:0 0 0 30px;
  font-size:16px;
  font-weight:bold;
  a{
    color:#000;
  }
`
