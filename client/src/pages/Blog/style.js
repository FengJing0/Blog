import styled from "styled-components"

export const Time = styled('p')`
  margin:0;
  font-size:24px;
`

export const TimeItem = styled('p')`
  margin:0 0 0 50px;
  font-size:28px;
  a{
    color:#000;
  }
`
